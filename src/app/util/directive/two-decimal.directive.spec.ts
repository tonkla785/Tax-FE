import { TwoDecimalDirective } from './two-decimal.directive';

describe('TwoDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
